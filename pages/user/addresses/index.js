import UserBody from "@/components/user-body";

export default function Addresses() {
  return (
    <UserBody>
      <div>
        <h2 className="oswald uppercase text-4xl">Địa chỉ đã lưu</h2>
        <span>hiện chưa có địa chỉ nào được lưu</span>
      </div>
    </UserBody>
  );
}
